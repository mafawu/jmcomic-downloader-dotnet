<UserControl x:Class="JmComic.App.Views.NovelSearchPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource SurfaceBrush}">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="18,16,18,12">
            <StackPanel Orientation="Horizontal">
                <Border Background="#7C6CF6" CornerRadius="6" Padding="6,2" VerticalAlignment="Center"><TextBlock Text="小" FontSize="10" Foreground="White" FontWeight="Bold"/></Border>
                <TextBlock Text="小说筛选" FontSize="15" FontWeight="Bold" Margin="8,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>
        <Border Grid.Row="1" Margin="14,0,14,12" CornerRadius="8" Background="{DynamicResource HoverBgBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1">
            <Grid>
                <TextBox x:Name="KeywordBox" BorderThickness="0" Background="Transparent" Padding="12,9" FontSize="12.5" TextChanged="KeywordBox_TextChanged" GotFocus="KeywordBox_GotFocus" LostFocus="KeywordBox_LostFocus"/>
                <TextBlock x:Name="PlaceholderText" Text="搜索小说名 / 标签" FontSize="12" Foreground="{DynamicResource TextDisabledBrush}" Margin="13,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False"/>
                <Button x:Name="ClearButton" Style="{StaticResource IconButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,4,0" Click="ClearButton_Click" Visibility="Collapsed">
                    <Path Data="{StaticResource IconClose}" Stroke="{DynamicResource TextPrimaryBrush}" StrokeThickness="1.6" Stretch="Uniform" Width="11" Height="11"/>
                </Button>
            </Grid>
        </Border>
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="14,0,14,14">
            <StackPanel x:Name="TreeHost"/>
        </ScrollViewer>
    </Grid>
</UserControl>
